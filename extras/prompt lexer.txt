@workspace
Minishell Project Documentation

Introduction:
Minishell is a project aimed at creating a simple shell, similar to Bash, providing insights into processes and file descriptors. This educational project is part of a coding school's curriculum, emphasizing hands-on learning in system programming by building a functional command line interpreter.

Common Instructions:
Language: The project must be written in C.
Code Norms: Compliance with specific coding standards (referred to as "Norm") is required. Any norm violation in bonus files/functions results in a zero score.
	-Functions CANNOT be more than 25 lines
  -No tenary operators
Memory Management: No memory leaks will be tolerated. All heap allocated spaces must be freed appropriately.
Makefile:
  - Must compile the project using `cc` with flags `-Wall`, `-Wextra`, and `-Werror`.
  - Must include rules: `$(NAME)`, `all`, `clean`, `fclean`, and `re`.
  - If `libft` is allowed, it must be compiled and included correctly.

Mandatory Part

Program Name: `minishell`
Files to Turn In: `Makefile`, `*.h`, `*.c`
External Functions allowed (Only these external functions are allowed, all other functions need to be written) (STROK() not allowed):
  - `readline`, `rl_clear_history`, `rl_on_new_line`, `rl_replace_line`, `rl_redisplay`, `add_history`, `printf`, `malloc`, `free`, `write`, `access`, `open`, `read`, `close`, `fork`, `wait`, `waitpid`, `wait3`, `wait4`, `signal`, `sigaction`, `sigemptyset`, `sigaddset`, `kill`, `exit`, `getcwd`, `chdir`, `stat`, `lstat`, `fstat`, `unlink`, `execve`, `dup`, `dup2`, `pipe`, `opendir`, `readdir`, `closedir`, `strerror`, `perror`, `isatty`, `ttyname`, `ttyslot`, `ioctl`, `getenv`, `tcsetattr`, `tcgetattr`, `tgetent`, `tgetflag`, `tgetnum`, `tgetstr`, `tgoto`, `tputs`.

Functionality:
  - Display a command prompt and handle command input with history.

  - Implement signal handling with minimal global variables.
  - Handle ctrl-C, ctrl-D and ctrl-\ which should behave like in bash.
  - In interactive mode: ctrl-C displays a new prompt on a new line. ctrl-D exits the shell. ctrl-\ does nothing.
  
  - Search and launch the right executable (based on the PATH variable or using a relative or an absolute path).
  - Not interpret unclosed quotes or special characters which are not required by the subject such as \ (backslash) or ; (semicolon).
  - Handle ’ (single quote) which should prevent the shell from interpreting the meta-characters in the quoted sequence.
  - Handle " (double quote) which should prevent the shell from interpreting the meta-characters in the quoted sequence except for $ (dollar sign).
  - Manage input/output redirection and piping.
  - Handle environment variables ($ followed by a sequence of characters) which should expand to their values.
  - Handle $? which should expand to the exit status of the most recently executed foreground pipeline.
  - Implement built-in commands `echo`(with option -n), `cd` (with only a relative or absolute path), `pwd`, `export`, `unset`, `env`, and `exit`.

When generating code make sure to add a place for all the functionality needed in the project, for example when generating code for parsing it has to accomodate for all features that have to be parsed before executing. since the program works using user input for almost all features the code for parsing needs to accomodate for all those features.

Here is what i need you to help me with:

i need to create a lexer function that takes in a char *input and creates a linked list.

the lexer function should use the struct t_lexer and it will be filled as follows:

typedef struct s_lexer
{
	char			*str; = (further explanations will follow)
	t_token_type	token; = NONE or PIPE
	int				index; = index of node (starts from 0)
	struct s_lexer	*prev; = previous node in list pointer (null if first node)
	struct s_lexer	*next; = next node in list pointer (null if last node)
}					t_lexer;

the char *str will be set with the following rules:
it will be filled with all the characters within pipes (pipes inside "" and '' should be treated as normal char)
not treat unclosed quotes
if the char is a pipe (|) and its not between quotes it will be set to null

the list should process the input like this:

Cmd1 -flag1 arg1 "arg | 2" | Cmd2 -flag2 "arg | 2 arg | cmd3

node 0: Cmd1 -flag1 arg1 "arg | 2"
node 1: PIPE
node 2: Cmd2 -flag2 "arg | 2 arg | cmd3

You have completed the previous task. now i need you to create a parsing function
that takes in a t_lexer *head and creates a linked list in which each node is one of
the str nodes from the lexer, the pipes should be skipped. 

the parsing function should use the struct t_cmd and it will be filled as follows
for every str node in the lexer struct:

typedef struct s_cmd
{
	char			*cmd; the first word of the str

	char			**argv; all words separated by whitespace between the first word and a redirection or pipe

	char			*infile; word after < "HERE_DOC" if word is after <<

	char			*outfile; word after > or >>

	char			*delimiter; the str between whitespaces after << (if found, null if there is no <<) can also be inside "" and ''

	struct s_cmd	*prev; = previous node in list pointer (null if first node)

	struct s_cmd	*next; = ext node in list pointer (null if last node)

	int				append; = 1 if >> is found 0 if not (the file can be atached to the >>file.txt) and as usual ignore if it is inside "" or ''
}					t_cmd;
